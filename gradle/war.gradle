apply plugin: "war"

bootWar {
    mainClassName = "dev.nortech.jhipster.JHipsterApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
