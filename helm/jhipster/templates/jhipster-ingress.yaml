apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jhipster
  namespace: production
spec:
  rules:
  - host: production.34.98.92.186.nip.io
    http:
      paths:
      - path: /*
        backend:
          serviceName: jhipster
          servicePort: 80
