apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jhipster
  namespace: application
spec:
  rules:
  - host: jhipster.application.35.244.130.75.nip.io
    http:
      paths:
      - path: /*
        backend:
          serviceName: jhipster
          servicePort: 80
