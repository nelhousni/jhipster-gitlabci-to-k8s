apiVersion: v1
kind: Service
metadata:
  name: jhipster
  namespace: production
  labels:
    app: jhipster
spec:
  selector:
    app: jhipster
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 8080
